n = int(input())
x= []
y = []
for i in range(n):
    x1, y1 = map(int, input().split())
    x.append(x1)
    y.append(y1)
x = list(dict.fromkeys(x))
y = list(dict.fromkeys(y))
print(min(len(x), len(y)))
